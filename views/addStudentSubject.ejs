<!DOCTYPE html>
<html lang="en">
<head>
  <title>Add Subject to Student</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
</head>
<body>
  <br>
  <div container="w-50 container">
    <h1 align="center">Add Subject to Student</h1>
    <table align="center" class="table table-sm w-25">
      <br>
      <thead></thead>
      <tbody>
        <tr>
          <td><b>First Name</b></td>
          <td><%= data.firstName %></td>
        </tr>
        <tr>
          <td><b>Last Name</b></td>
          <td><%= data.lastName %></td>
        </tr>
        <tr>
          <td><b>Email</b></td>
          <td><%= data.email %></td>
        </tr>
        <tr>
          <td>
            <form action="/students/<%= data.id%>/add-subject" method="POST">
            <select name="SubjectId">
              <% subjData.forEach( a => {%>
              <option value="<%= a.id %>"><%= a.subjectName %></option>
              <% }) %>
            </select>
          </tr>
          <tr>
            <td>
              <button class="btn btn-primary" type="submit">Add Subject</button>
            </td>
            </form>
          </tr>
          </td>
      </tbody>
    </table>
  </div>
</body>
</html>